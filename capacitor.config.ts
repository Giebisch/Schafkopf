import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.rafael-giebisch',
  appName: 'Schafkopf',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
